socket_path = /tmp/wd-broker-test.sock
wd_timeout_s = 10

# Thise is should be the bad line, must be true or false
strict_clients = bad-value

unique_clients = false

[client alpha]
timeout_ms = 5000

[client beta]
timeout_ms = 5000