socket_path = /tmp/wd-broker-test.sock

# Thise is should be the bad line, timeout to low
wd_timeout_s = 1

strict_clients = false
unique_clients = false

[client alpha]
timeout_ms = 5000

[client beta]
timeout_ms = 5000