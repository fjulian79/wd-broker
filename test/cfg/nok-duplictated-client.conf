socket_path = /tmp/wd-broker-test.sock
wd_timeout_s = 10
strict_clients = true
unique_clients = true

[client alpha]
timeout_ms = 5000

[client beta]
timeout_ms = 5000

# Thise is should be the bad line, because of the unknown key
[client beta]
timeout_ms = 5000
