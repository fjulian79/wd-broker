socket_path = /tmp/wd-broker-test.sock
wd_timeout_s = 10
strict_clients = false

# Thise is should be the bad line, must be true or false
unique_clients = bad-value

[client alpha]
timeout_ms = 5000

[client beta]
timeout_ms = 5000